<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Protection Group</title>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            max-width: 500px;
            width: 100%;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 24px;
            font-weight: 500;
            color: #333;
        }

        label {
            display: block;
            margin-top: 20px;
            font-size: 16px;
            color: #555;
        }

        input[type="text"] {
            width: 93%;
            padding: 12px 15px;
            margin-top: 8px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 6px;
            outline: none;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus {
            border-color: #007bff;
        }

        input[type="submit"] {
            width: 100%;
            padding: 12px 15px;
            margin-top: 20px;
            font-size: 16px;
            font-weight: 500;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #0056b3;
        }

        .import-btn {
            width: 100%;
            padding: 12px 15px;
            font-size: 16px;
            font-weight: 500;
            color: #fff;
            background-color: #28a745;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .import-btn:hover {
            background-color: #218838;
        }

        .file-input {
            display: block;
            margin: 10px 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <h2>Add Protection Group</h2>
        <form action="/protection/add-protection-group" method="POST">
            <label for="name">Name</label>
            <input type="text" id="name" name="name" placeholder="Enter Protection Group Name" required>

            <input type="submit" value="Add Protection Group">
        </form>

        <form action="/protection/import-csv" method="POST" enctype="multipart/form-data" style="margin-top: 20px;">
            <input type="file" name="csvFile" accept=".csv" required class="file-input">
            <button type="submit" class="import-btn">Import CSV</button>
        </form>
    </div>
</body>

</html>
